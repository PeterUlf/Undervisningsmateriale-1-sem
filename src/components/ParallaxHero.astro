<section class="parallax-hero">
  <img src="https://images.unsplash.com/photo-1451187580459-43490279c0fa?q=80&w=2072&auto=format&fit=crop" alt="Space" class="parallax-bg" />
  <div class="hero-content">
    <h2>Parallax Hero</h2>
    <p>Baggrunden bevæger sig langsommere end forgrunden, hvilket skaber dybde.</p>
  </div>
</section>

<style>
.parallax-hero {
  position: relative;
  height: 500px;
  overflow: hidden;
  display: grid;
  place-items: center;
  color: white;
  text-align: center;
  border-radius: 8px;
  margin: 2rem 0;
}

.parallax-bg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 140%; /* Højere end containeren for at kunne bevæge sig */
  object-fit: cover;
  z-index: -1;
  
  /* Animationen koblet til scroll */
  animation: parallax-move linear both;
  animation-timeline: view();
  animation-range: entry 0% exit 100%;
}

.hero-content {
  background: rgba(0,0,0,0.6);
  padding: 2rem;
  border-radius: 1rem;
  backdrop-filter: blur(5px);
  border: 1px solid rgba(255,255,255,0.2);
}

.hero-content h2 {
  color: white !important;
  margin-top: 0;
}

@keyframes parallax-move {
  from { transform: translateY(-15%); }
  to { transform: translateY(0%); }
}
</style>
