---
title: Sortering i JavaScript
description: Lær at sortere arrays med sort() og brug det i dit projekt
sidebar:
  label: Sortering
  order: 4.18
  badge:
    text: Tema 8
    variant: tip
    class: badge-theme-8
---

import Slide from "../../../components/Slide.astro";

<Slide>

## Hvad lærer du her?

Du lærer:

1. Hvordan `sort()` virker
2. Forskellen på tal-sortering og tekst-sortering
3. Hvordan du bygger sortering ind i dit projekt (fx produktliste)

</Slide>
<div class="page-break"></div>
<Slide>

## Opgaver: Overblik

1. Skriv med egne ord, hvad sortering betyder i en produktliste.
2. Nævn to sorteringer, I vil have i jeres projekt (fx pris og navn).
3. Beslut om listen skal starte usorteret eller med en standard-sortering.

</Slide>
<div class="page-break"></div>
<Slide>

## Grundidéen i `sort()`

`sort()` sammenligner to elementer ad gangen.

Med tal bruger du en sammenligningsfunktion:

```javascript
const prices = [799, 299, 1299, 499];

const lowToHigh = [...prices].sort((a, b) => a - b);
const highToLow = [...prices].sort((a, b) => b - a);

console.log(lowToHigh); // [299, 499, 799, 1299]
console.log(highToLow); // [1299, 799, 499, 299]
```

Vigtigt: `sort()` ændrer original-arrayet, så brug ofte en kopi: `[...array]`.

Det betyder i praksis:

1. Det er den nye variabel (fx `lowToHigh`), der er det nye array.
2. Det nye array laves fra en kopi af originalen (`[...prices]`).
3. Originalen (`prices`) beholder sin oprindelige rækkefølge.
4. Objekter/værdier er de samme, men rækkefølgen ligger i et nyt array.

</Slide>
<div class="page-break"></div>
<Slide>

## Hvad gør `.sort((a, b) => a - b)`?

Når du skriver:

```javascript
const lowToHigh = [...prices].sort((a, b) => a - b);
```

sker der dette:

1. `sort()` tager to tal ad gangen fra arrayet og kalder dem `a` og `b`.
2. Funktionen returnerer resultatet af `a - b`.
3. Det resultat fortæller `sort()`, hvem der skal stå først.

Reglen er:

1. Negativt resultat: `a` skal stå før `b`.
2. Positivt resultat: `a` skal stå efter `b`.
3. `0`: de er lige store, så rækkefølgen mellem dem er ligegyldig.

Eksempler på sammenligninger:

1. `a = 299`, `b = 799` -> `a - b = -500` -> `299` placeres før `799`.
2. `a = 1299`, `b = 499` -> `a - b = 800` -> `1299` placeres efter `499`.
3. `a = 450`, `b = 450` -> `a - b = 0` -> de er ens.

Vigtigt at vide:

1. `sort()` laver mange sammenligninger internt, indtil hele arrayet er i orden.
2. Du styrer ikke rækkefølgen af sammenligningerne, kun reglen for sammenligning.
3. `a - b` giver stigende sortering (billigst til dyrest).
4. `b - a` vender den om (dyrest til billigst).

</Slide>
<div class="page-break"></div>
<Slide>

## Opgaver: `sort()` med tal

Brug dette array:

```javascript
const prices = [1200, 450, 799, 250, 999];
```

1. Sorter fra billigst til dyrest.
2. Sorter fra dyrest til billigst.
3. Log både original-arrayet og det sorterede array.
4. Forklar hvorfor `[...prices]` er en god idé.

Udfyld koden her:

```javascript
const prices = [1200, 450, 799, 250, 999];

const lowToHigh = ......UDFYLD HER........
const highToLow = ..........UDFYLD HER............

console.log("Original:", prices);
console.log("Billigst -> dyrest:", lowToHigh);
console.log("Dyrest -> billigst:", highToLow);
```

Tjek resultatet i DevTools -> Console.

</Slide>
<div class="page-break"></div>
<Slide>

## Sortering af tekst (A-Å)

Når du sorterer tekst, er `localeCompare()` ofte den bedste løsning:

```javascript
const names = ["Zebra", "Abe", "Ørn", "And"];

const az = [...names].sort((a, b) => a.localeCompare(b, "da"));
const za = [...names].sort((a, b) => b.localeCompare(a, "da"));

console.log(az); // A-Å
console.log(za); // Å-A
```

`"da"` giver dansk sortering, så fx `Æ`, `Ø`, `Å` håndteres bedre.

</Slide>
<div class="page-break"></div>
<Slide>

## Opgaver: Tekstsortering

Brug dette array:

```javascript
const brands = ["Zeta", "Alpha", "ØkoWear", "Basic", "Århus Style"];
```

1. Sorter brands A-Å med `localeCompare(..., "da")`.
2. Sorter brands Å-A.
3. Test forskellen på sortering med og uden `"da"`.
4. Skriv en kort note om, hvilken version der virker bedst for dansk tekst.

</Slide>
<div class="page-break"></div>
<Slide>

## Eksempel: Knap til sortering efter pris

Hvis I allerede har en fetch-løsning som denne, kan I tilføje en knap der
sorterer listen fra billigst til dyrest.

HTML:

```html
<button id="sortByPriceBtn">Sorter efter pris</button>
<section id="productListContainer"></section>
```

JavaScript:

```javascript
const listURL = "https://kea-alt-del.dk/t7/api/products";
const listContainer = document.querySelector("#productListContainer");
const sortByPriceBtn = document.querySelector("#sortByPriceBtn");

let allProducts = [];

function getProducts() {
  fetch(listURL)
    .then((res) => res.json())
    .then((products) => {
      allProducts = products; // gem originaldata
      showProducts(allProducts);
    });
}

function showProducts(products) {
  listContainer.innerHTML = "";

  products.forEach((product) => {
    listContainer.innerHTML += `
      <article class="productCard">
        <h3>${product.productdisplayname}</h3>
        <p>Pris: ${product.price}</p>
      </article>
    `;
  });
}

function sortByPriceAsc() {
  const sorted = [...allProducts].sort((a, b) => a.price - b.price);
  showProducts(sorted);
}

sortByPriceBtn.addEventListener("click", sortByPriceAsc);
getProducts();
```

Forskellen fra den kode I kommer fra:

1. Før hentede I data og viste dem direkte med `showProducts(products)`.
2. Nu gemmer vi også data i `allProducts`, så vi kan bruge dem igen senere.
3. Før havde I ingen brugerhandling efter fetch.
4. Nu har I en knap (`sortByPriceBtn`), som kan ændre visningen uden at hente API-data igen.
5. Den nye funktion `sortByPriceAsc()` laver en sorteret kopi og sender den til `showProducts(...)`.
6. `showProducts(...)` er stadig den samme renderer. Forskellen er, at den nu kan få både usorterede og sorterede data.

Kort om `shallow copy`:

1. `sorted` er et nyt array.
2. `sorted` bliver lavet ud fra en kopi: `[...allProducts]`.
3. Den kopi bliver sorteret med `.sort((a, b) => a.price - b.price)`.
4. `allProducts` beholder sin oprindelige rækkefølge.
5. Objekterne inde i arrayet er de samme objekter, men rækkefølgen ligger i et nyt array.

Sådan forklarer du det til studerende:

1. Vi gemmer API-data i `allProducts`.
2. Knappen kalder `sortByPriceAsc()`.
3. Vi sorterer en kopi med `[...allProducts]`.
4. Vi viser den sorterede liste med `showProducts(...)`.

[Filtrering i JavaScript →](/javascript/filtrering/)

</Slide>
